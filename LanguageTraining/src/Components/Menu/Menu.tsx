import './Menu.css';

export const Menu = () => {
    return (
        <nav className="menu">
            <section className="menuSection">
                <a href="/">Home</a>
            </section>
        </nav>
    )
}